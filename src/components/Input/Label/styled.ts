import styled from "styled-components";

export const S = {
  Label: styled.label`
    width: 120px;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    font-weight: 500;
    font-size: 17px;
    color: var(--black);
    border-bottom: 2px solid var(--black);
  `,
};
